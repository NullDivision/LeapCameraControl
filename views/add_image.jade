extends ./registry.jade

block content
    .form-box
        form(method='post')
            p Add a photo url to the list of feeds
            .form-group
                input(name='url', class='form-control')
            .form-group
                button(type='submit', class='btn btn-default btn-sm') Submit
